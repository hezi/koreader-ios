local _ = require("gettext")
return {
    fullname = _("iOS file picker"),
    description = _([[Adds cloud folders (iCloud Drive, Dropbox, etc.) to the file browser via iOS' native document picker.]]),
}
